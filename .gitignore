data/~$*.xlsx
.DS_Store
node_modules/

# Real secrets — never committed. .env.example (blank values) stays tracked as the template.
.env

# Real data lives here locally once you start using Ledger for real — kept out of git so a
# future commit in this repo can't accidentally push real financial data. The versions already
# committed (sample/demo data) stay in git history; local edits from here on don't sync.
data/expenses.xlsx
data/log.json
data/learnings.json
tasks.json
notes/
logs/
scripts/seed-expenses-from-dashboard.js

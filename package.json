{
  "name": "ledger",
  "private": true,
  "version": "1.0.0",
  "description": "Ledger — a personal bookkeeping bot with a web UI.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node app/server.js",
    "db:check": "node scripts/db-check.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "pg": "^8.23.0"
  }
}
